#!/usr/bin/env bash
protected_branch='main'
current_branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$current_branch" = "$protected_branch" ]; then
  echo "ERROR: direct push to '$protected_branch' is blocked. Open a PR from a feature branch."
  exit 1
fi
